#include "main.h"
/**
 * _isaplha - checks for alphabets
 * @c: argument
 * Return: 0
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (C >= 'A' && C <= 'Z'))
	{
		return (1);
	}
	else
		return (0);
}
 
